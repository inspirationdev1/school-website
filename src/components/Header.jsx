const Header = () => {
  return (
    <header className="header">
      <h2>Inspiration School Management System</h2>
      <nav>
        <a href="#home">Home</a>
        <a href="#features">Features</a>
        <a href="#pricing">Pricing</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  );
};

export default Header;
